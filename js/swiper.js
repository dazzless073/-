var swiper = new Swiper('.swiper-container', {
    effect: 'flip',
    speed: 500,
    grabCursor: true,
    navigation: {
      nextEl: '.swiper-button-next',
    },
    scrollbar: {
      el: '.swiper-scrollbar',
    },
  });